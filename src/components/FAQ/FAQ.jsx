
const FAQ = ({ id, faqtion, answer }) => {

    return (
        <div>
            <div>
                <h2 className="text-2xl font-semibold">{faqtion}</h2>
            </div>
        </div>
    );
};

export default FAQ;